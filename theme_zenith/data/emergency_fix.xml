<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="0">
        <!-- Emergency deletion of the broken view to force full recreation -->
        <!-- We use 'search' to find it by key/xml_id if possible, or by ID if it was an external ID -->
        <!-- Since we want to clear the 'theme_zenith.zenith_breadcrumbs' view specifically: -->
        
        <!-- Clean slate for V2 just in case -->
        <delete id="theme_zenith.zenith_breadcrumbs_v2" model="ir.ui.view"/>
        <!-- Kill the old zombie view (ID deletion) -->
        <delete id="theme_zenith.zenith_breadcrumbs" model="ir.ui.view"/>
        
        <!-- Fix for the persistent sticky cta / buy now button error -->
        <!-- We delete them to force Odoo to recreate them from the files without database overrides -->
        <!-- FIX PERSISTENT BRANDING (Nuclear Option) -->
        <!-- This finds ANY view or website name with 'tecnosoft' and destroys/fixes it -->
        <function model="website" name="_clean_zombie_views"/>
        <delete id="theme_zenith.zenith_sticky_cta" model="ir.ui.view"/>
        <delete id="theme_zenith.zenith_buy_now_button" model="ir.ui.view"/>
        
    </data>
</odoo>
