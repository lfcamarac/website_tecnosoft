<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Premium Variant Selectors Override -->
    <!-- Premium Variant Selectors Override (DISABLED: XPath errors in Odoo 18) -->
    <!--
    <template id="tecnosoft_variants" inherit_id="website_sale.variants" name="Tecnosoft Premium Variants">
        < ! - - We use XPath to wrap attributes in a more styled container if needed, 
             but mostly SCSS handles the styling of standard Odoo classes.
             If we want custom behavior (like image swatches), we might need to change the template logic. - - >
        <xpath expr="//ul[hasclass('js_add_cart_variants')]" position="attributes">
            <attribute name="class" add="tecnosoft-variant-list border-0 p-0" separator=" "/>
        </xpath>
        
        < ! - - Customize Color Attribute rendering if we want to force a specific look - - >
        <xpath expr="//label[hasclass('o_variant_pills_input_value')]" position="attributes">
            <attribute name="t-attf-class" add="rounded-circle shadow-sm" separator=" "/>
            <attribute name="t-att-data-preview-image">
                '/web/image/product.attribute.value/%s/image_128' % ptav.product_attribute_value_id.id if ptav.product_attribute_value_id.image_128 else None
            </attribute>
        </xpath>
    </template>
    -->
</odoo>
